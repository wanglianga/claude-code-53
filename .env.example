# ---- 必填：宿主机发布端口（评审环境通过环境变量注入）----
CC_PUBLISH_PORT=3000

# ---- 可选：覆盖默认值 ----
# 数据库（仅 compose 内部网络使用，不发布到宿主）
POSTGRES_USER=ortho
POSTGRES_PASSWORD=ortho_pass
POSTGRES_DB=ortho

# API JWT 密钥（生产环境务必修改）
JWT_SECRET=ortho-dev-secret-change-me
